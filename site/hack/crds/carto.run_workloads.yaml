---
_version: v1alpha1
spec:
  build:
    env:
      # really should be in the docs
      _appendDescription: See https://kubernetes.io/docs/tasks/inject-data-application/environment-variable-expose-pod-information/
      _hideChildDescriptions: true
      _maxDepth: 1
  env:
    # really should be in the docs
    _appendDescription: See https://kubernetes.io/docs/tasks/inject-data-application/environment-variable-expose-pod-information/
    _hideChildDescriptions: true
  resources:
    _hideChildDescriptions: true